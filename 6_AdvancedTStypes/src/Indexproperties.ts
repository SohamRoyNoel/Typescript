// /**
//  * If you need a interface that implements all types of error:
//  * But you don't want to define it explicitly
//  */

// interface ErrorInterface {
//   [props: string] : string // [props: string] =>> Means the type of key || :string =>> type of message
// }

// const getError: ErrorInterface = {
//   email: 'Invalid email'  
// }

// console.log(getError);